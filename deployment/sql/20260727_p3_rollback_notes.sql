-- =============================================================================
-- P3 回滚建议（仅人工参考；不含任何自动 DROP/DELETE，不删除业务数据）
-- -----------------------------------------------------------------------------
-- 前置条件：
--   1. 已停止所有 P3 代码部署；
--   2. 已对数据库做完整备份（mysqldump 或物理快照）并验证可恢复。
-- 执行顺序（先删子表 opportunity_stage，再删父表 opportunity，尊重逻辑引用）：
--   DROP TABLE IF EXISTS `5kcrm_opportunity_stage`;
--   DROP TABLE IF EXISTS `5kcrm_opportunity`;
-- 数据影响说明：
--   - 这两张表为 P3 新增、不承载既有历史业务数据；
--   - 由 P3 代码实际写入的机会/阶段记录不自动删除，如需清理须人工审核后处理；
--   - 不影响 5kcrm_crm_leads / 5kcrm_crm_customer 等既有表结构与数据。
-- 不可回滚的内容：本文件不提供自动脚本；任何数据删除须人工显式执行。
-- =============================================================================
