-- =============================================================================
-- pconfig 回滚建议（仅人工参考；不含任何自动 DROP/DELETE，不删除业务数据）
-- -----------------------------------------------------------------------------
-- 前置条件：已停止依赖该配置的代码（P3 月度上限 / P4 池拆分 / P5 奖励金额来源）；已做数据库备份。
-- 执行选项（按需二选一，均人工显式执行）：
--   A) 仅清空配置值（保留表结构，重置为"待配置"）：
--        UPDATE 5kcrm_reward_config SET config_value = NULL WHERE config_key IN
--          ('monthly_cap_amount','dealer_first_payment_reward','hospital_stage_rewards','outsource_pool_split');
--   B) 完全移除配置表：
--        DROP TABLE IF EXISTS `5kcrm_reward_config`;
-- 数据影响说明：
--   - 配置表为 pconfig 新增；值为人工后台设置的金额/上限，不含历史业务数据；
--   - 清空后业务侧相关金额/上限将重新显示"待配置"并阻断奖励/结算/自动计算（符合"未配置不发放"）；
--   - 不影响既有业务表。
-- 不可回滚的内容：本文件不提供自动脚本；任何配置值清空须人工显式执行并记录。
-- =============================================================================
