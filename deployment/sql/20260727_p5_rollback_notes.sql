-- =============================================================================
-- P5 回滚建议（仅人工参考；不含任何自动 DROP/DELETE，不删除业务数据）
-- -----------------------------------------------------------------------------
-- 前置条件：已停止 P5 代码；已做完整数据库备份并验证可恢复。
-- 执行顺序（尊重逻辑引用：offset→candidate→batch；business_expense 独立）：
--   DROP TABLE IF EXISTS `5kcrm_reward_offset`;        -- 引用 candidate
--   DROP TABLE IF EXISTS `5kcrm_reward_candidate`;     -- 引用 batch(batch_id)
--   DROP TABLE IF EXISTS `5kcrm_reward_batch`;
--   DROP TABLE IF EXISTS `5kcrm_business_expense`;     -- 独立商务费用流程
-- 数据影响说明：
--   - 四表为 P5 新增；不承载既有历史业务数据；
--   - 奖励候选/批次/冲销/商务费用记录不自动删除，如需清理须人工审核（涉及金额，务必复核）；
--   - 不影响既有员工/财务表。
-- 不可回滚的内容：本文件不提供自动脚本；奖励/费用数据删除属高风险，必须人工显式执行。
-- =============================================================================
