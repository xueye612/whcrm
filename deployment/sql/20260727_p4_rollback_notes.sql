-- =============================================================================
-- P4 回滚建议（仅人工参考；不含任何自动 DROP/DELETE，不删除业务数据）
-- -----------------------------------------------------------------------------
-- 前置条件：已停止 P4 代码；已做完整数据库备份并验证可恢复。
-- 执行顺序（先删分配子表 reward_distribution，再删外包档案 outsource_project）：
--   DROP TABLE IF EXISTS `5kcrm_reward_distribution`;
--   DROP TABLE IF EXISTS `5kcrm_outsource_project`;
-- 数据影响说明：
--   - 两表为 P4 新增、不承载既有历史业务数据；
--   - P4 代码写入的档案/分配记录不自动删除，如需清理须人工审核；
--   - 不影响 5kcrm_work 等既有表。
-- 不可回滚的内容：本文件不提供自动脚本。
-- =============================================================================
