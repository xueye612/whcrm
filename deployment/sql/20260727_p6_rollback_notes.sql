-- =============================================================================
-- P6 回滚建议（仅人工参考；不含任何自动 DROP/DELETE，不删除业务数据）
-- -----------------------------------------------------------------------------
-- 前置条件：已停止 P6 代码；已做完整数据库备份并验证可恢复。
-- 执行顺序（两表相互独立，无逻辑引用，顺序不限）：
--   DROP TABLE IF EXISTS `5kcrm_responsibility_case`;
--   DROP TABLE IF EXISTS `5kcrm_performance`;
-- 数据影响说明：
--   - 两表为 P6 新增；不承载既有历史业务数据；
--   - 绩效汇总/责任认定记录不自动删除；责任认定涉及个人，如需清理须人工审核；
--   - 不影响既有员工/任务表。
-- 不可回滚的内容：本文件不提供自动脚本。
-- =============================================================================
