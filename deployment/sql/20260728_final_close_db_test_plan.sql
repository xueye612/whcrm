-- ============================================================
-- 独立测试库双执行 + 危险引用注入验证脚本
-- 执行步骤（runner）：
--   1) 从正式库单事务只读备份恢复到独立测试库 crm_kilo_test_<timestamp>
--   2) 执行全部 8 个 final_* forward 两次
--   3) 比较：结构、行数、规则摘要
--   4) 注入危险引用（type_id=6 商机引用 + crm_rianjp 扩展数据 + dealer_customer_id 非 0 商机）
--   5) 再次执行 forward，验证硬阻断触发且不删除
--   6) 验证结束后删除测试库和临时备份
-- ============================================================
--
-- 本文件作为人工执行步骤参考，不自动执行；由 runner shell 完成。
SELECT 'final_close_db_test_plan_note' AS note;
