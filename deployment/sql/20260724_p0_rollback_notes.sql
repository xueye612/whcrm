-- =============================================================================
-- P0 回滚建议（仅策略说明，不承诺数据回滚）
-- =============================================================================
-- 回滚原则：停止新代码部署 → 保留新增数据 → 可安全移除新增约束/结构。
--
-- 安全回滚步骤（在停止 P0 代码后执行）：
--
-- 1. 移除 P0 新增扩展表（这些表不承载历史业务数据，删除不影响旧任务/台账读取）：
--    DROP TABLE IF EXISTS `5kcrm_task_test_history`;
--    DROP TABLE IF EXISTS `5kcrm_task_test_ext`;
--    DROP TABLE IF EXISTS `5kcrm_task_wrk_log`;
--    DROP TABLE IF EXISTS `5kcrm_task_transition_log`;
--    DROP TABLE IF EXISTS `5kcrm_task_workflow`;
--
-- 2. customer_ledger 新增列的处理（谨慎）：
--    - work_id / class_id / task_id：若迁移前生产库本无这些列、且无历史数据依赖它们，
--      可移除；若迁移前已存在（字段漂移），则不可移除。
--    - auto_task_key：P0 新增的幂等键列，可在确认无依赖后移除。
--    - 建议保留列、仅停止写入，优先通过停止代码而非删列实现回滚。
--
-- 3. 不可回滚的内容：
--    - 任何由 P0 代码实际生成的业务记录（任务、测试任务、审计日志）不自动删除，
--      如需清理须由有权限人员逐条审核后人工处理。
--    - 本文件不包含任何 DELETE / TRUNCATE / DROP TABLE IF EXISTS 的自动执行语句，
--      避免误删生产数据。
--
-- 部署前硬阻塞（必须满足才能上线）：
--    - 已在备份副本或测试库上完整执行预检→前向→核验，且前向迁移二次执行全部 skip；
--    - 实库只读基线已确认 customer_ledger 字段漂移情况；
--    - 已有可用的数据库备份和恢复演练记录。
