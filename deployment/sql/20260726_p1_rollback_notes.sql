-- =============================================================================
-- P1 回滚建议（仅策略说明，不承诺数据回滚；不包含任何自动 DROP/DELETE）
-- =============================================================================
-- 回滚原则：停止 P1 代码部署 → 保留新增数据 → 可安全移除新增结构。
--
-- 安全回滚步骤（在停止 P1 代码后执行）：
--
-- 1. 移除 P1 新增表（这些表不承载历史业务数据，删除不影响旧项目读取）：
--    DROP TABLE IF EXISTS `5kcrm_work_knowledge_link`;
--    DROP TABLE IF EXISTS `5kcrm_work_member_contribution`;
--    DROP TABLE IF EXISTS `5kcrm_work_milestone`;
--    DROP TABLE IF EXISTS `5kcrm_work_profile`;
--
-- 2. 本迁移未对 5kcrm_work 增加列，无需回列。
--
-- 3. 不可回滚的内容：
--    - 由 P1 代码实际写入的档案/里程碑/贡献/链接记录不自动删除；
--      如需清理须由有权限人员审核后人工处理。
--    - 本文件不含任何 DELETE / TRUNCATE / DROP TABLE IF EXISTS 的自动执行语句。
--
-- 部署前硬阻塞（必须满足才能上线）：
--    - 已在备份副本或测试库上完整执行预检→前向→核验，且前向迁移二次执行全部跳过；
--    - 已有可用的数据库备份和恢复演练记录。
